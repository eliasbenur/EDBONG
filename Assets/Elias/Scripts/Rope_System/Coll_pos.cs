using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Coll_pos : MonoBehaviour {

    int point_num;
    float dist;

    public Coll_pos(int point_num_, float dist_)
    {
        point_num = point_num_;
        dist = dist_;
    }

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
